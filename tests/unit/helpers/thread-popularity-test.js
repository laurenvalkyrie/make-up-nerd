import { threadPopularity } from 'make-up-nerd/helpers/thread-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | thread popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = threadPopularity([42]);
  assert.ok(result);
});
